# A basic implementation of Server-side rendering in React app

implementing the core of SSR features like, lazy-loading routes, error boundaries, streaming over suspense data-fetching using React Router's data API,
Tailwindcss for styling, and much more.
